# 작업 기록

> 이 파일은 **로컬 Claude(파워셸·VS Code)가 씁니다.**
> 클라우드 대화에서 "작업기록 확인해줘" 하면 제가 읽고 다음 지시를 내립니다.
>
> 새 기록은 **맨 위에** 추가합니다.

---

## 2026-09-16 16:21 (로컬 실행)

### 한 일

**3단계 — 노래 만들기: 세 번 시도, 세 번째에 크레딧 벽에서 멈춤**

api키.json 에 키가 들어온 것을 확인하고(값은 읽지 않고 길이만 확인) 실행했습니다.
오류가 날 때마다 원인이 분명해서 고치고 다시 돌렸고, 매번 다른 단계까지 전진했습니다.

#### 1차 — 클라우드플레어 차단 (403)

```
[실패] 서버가 403 로 답했습니다.
{"type":"https://developers.cloudflare.com/support/troubleshooting/http-status-codes/cloudflare-1xxx-errors/error-1010/","title":"Error 1010: Access denied","status":403,"detail":"The site owner has blocked access based on your browser's signature.","instance":"a3be1ba02a09d1e5","error_code":1010,"error_name":"browser_signature_banned","error_category":"access_denied","ray_id":"a3be1ba02a09d1e5","timestamp":"2026-09-16T07:20:19Z","zone":"api.sunoapi.org","cloudflare_error":true,"retryable":false,"owner_action_required":true,"what_you_should_do":"**Do not retry.** Your user-agent has been banned by the site owner.","footer":"This error was generated by Cloudflare on behalf of the website owner."}
```

원인은 파이썬 `urllib` 이 기본으로 보내는 `Python-urllib/3.13` User-Agent 입니다.
클라우드플레어가 이걸 봇으로 보고 막습니다. 수노 API 형식 문제가 아니라 접속 자체가
막힌 것이라, `노래만들기.py` 의 `보내기()` 헤더에 평범한 브라우저 User-Agent 를
한 줄 넣었습니다.

```python
"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
              "AppleWebKit/537.36 (KHTML, like Gecko) "
              "Chrome/140.0.0.0 Safari/537.36",
```

이 수정으로 403 은 사라졌습니다. **이 한 줄은 계속 필요합니다.**

#### 2차 — callBackUrl 누락 (400)

```
[실패] 작업 번호를 못 받았습니다. 응답은 아래와 같습니다.
{
  "code": 400,
  "msg": "Please enter callBackUrl.",
  "data": null
}
```

클라우드플레어를 통과해 진짜 수노 API 가 답한 첫 응답입니다.
`api.sunoapi.org` 는 `callBackUrl` 을 필수로 받습니다.
스크립트에 이미 `곡.get("콜백주소")` 를 읽는 자리가 있어서
`곡정보.json` 에 아래를 넣었습니다.

```json
"콜백주소": "https://example.com/callback",
```

결과는 `record-info` 로 직접 조회해 받으므로 콜백을 실제로 수신할 필요는 없습니다.
형식 검사만 통과하면 됩니다.

> 곡정보-예시.json 에도 `콜백주소` 를 넣어두는 게 좋겠습니다.
> 다음 곡부터 같은 400 을 또 만나게 됩니다.

#### 3차 — 크레딧 부족 (429) ← 지금 여기서 멈춤

```
[실패] 작업 번호를 못 받았습니다. 응답은 아래와 같습니다.
{
  "code": 429,
  "msg": "The current credits are insufficient. Please top up.",
  "data": null
}
```

주소·형식·인증이 전부 맞아서 계정 잔액까지 도달했습니다.
**스크립트 문제가 아닙니다.** sunoapi.org 계정에 크레딧을 충전하면
그대로 다시 돌리면 됩니다.

**4단계** — `영상\` 폴더가 여전히 비어 있어 영상 만들기는 시작하지 않았습니다.

### 만들어진 파일

노래 파일은 아직 없습니다. 크레딧 부족으로 생성 요청이 접수되지 않았습니다.

고친 파일:

| 경로 | 무엇을 |
|---|---|
| `youtube-goldtv\도구\노래만들기.py` | `보내기()` 헤더에 User-Agent 추가 |
| `C:\골드TV\001-비우니-채워지더라\곡정보.json` | `콜백주소` 항목 추가 |

### 막힌 것

크레딧 부족(429). 오류 원문은 위 3차 항목에 그대로 붙여뒀습니다.

### 사용자가 해야 할 일

1. **sunoapi.org 계정에 크레딧 충전** — 이것만 되면 노래는 바로 나옵니다
2. 영상 클립을 `C:\골드TV\001-비우니-채워지더라\영상\` 에 넣기
3. 썸네일 배경을 `C:\골드TV\001-비우니-채워지더라\배경.jpg` 로 넣기
4. `git push` 인증 — 로컬에서 push 가 아직 안 올라갑니다 (아래 참고)

### 클라우드 세션에 남기는 말

`노래만들기.py` 의 User-Agent 수정과 `곡정보.json` 의 콜백주소는
둘 다 재현 가능한 문제라 `곡정보-예시.json` 과 문서에도 반영해 주시면 좋겠습니다.

---

## 2026-09-16 14:37 (로컬 실행)

### 한 일

**1단계 — 환경 준비**

- `python --version` → `Python 3.13.5`. 설치돼 있음
- `0-처음설치.bat` 은 **실행하지 않고 내용만 확인한 뒤 개별 점검**했습니다.
  bat 끝에 `pause` 가 있어 자동 실행 환경에서 멈추고, 안에서 도는
  winget ffmpeg 설치도 이미 끝나 있었기 때문입니다. 실제 상태는 아래와 같습니다.
  - `pillow 11.3.0` 설치됨
  - `google-api-python-client 2.174.0` 설치됨
  - `google-auth-oauthlib 1.2.2` 설치됨
  - `requests 2.32.4` 설치됨
  - 즉 bat 이 깔려던 것은 전부 이미 있습니다
- `ffmpeg -version` → `ffmpeg version 9.0.1-full_build-www.gyan.dev`. 정상 동작
- 참고: 이 컴퓨터에 **git 이 아예 없어서** 저장소를 받기 전에
  `winget install --id Git.Git` 로 먼저 설치했습니다 (`git 2.55.0.windows.3`)
- `youtube-goldtv/도구/api키.json` 없음 → `{ "suno": "" }` 로 새로 만들었습니다.
  **키 값은 비어 있습니다.** 사장님이 직접 넣으셔야 합니다
- `git check-ignore` 로 확인한 결과 `api키.json` 은 `.gitignore` 에 걸려 있어
  커밋되지 않습니다

**2단계 — 첫 곡 작업 폴더**

- `C:\골드TV\001-비우니-채워지더라\영상\` 만들었습니다
- `곡정보.json` — `02-가사/001-비우니-채워지더라.md` 의 Song Title / Styles / Lyrics 를
  그대로 옮겼습니다. 모델 `V4_5PLUS`, api주소 `https://api.sunoapi.org`
- `업로드정보.json` — `03-업로드/001-비우니-채워지더라.md` 에서 가져왔습니다
  - 제목: 후보 중 **1번** `비우니 채워지더라 | 반야심경 색즉시공` 을 썼습니다.
    그 문서의 "최종 선택" 칸이 비어 있었고 1번이 추천이었기 때문입니다.
    다른 번호를 원하시면 바꿔드립니다
  - 태그 11개, 카테고리 10(음악)
  - **공개범위는 `private`** 로 넣었습니다. 03-업로드 문서에는 "공개"라고 적혀
    있었지만 지시사항의 "반드시 private" 을 따랐습니다
- `썸네일.json` — 윗줄 `비우니` / 아랫줄 `채워지더라` / 아래작은글 `반야심경`
- 세 파일 모두 파이썬 `json.load` 로 열어 읽히는 것까지 확인했습니다

**3단계 — 노래 만들기**

- 실행하지 않았습니다. `api키.json` 의 `suno` 값이 비어 있어
  지시사항의 조건("키가 들어 있으면 실행")에 맞지 않습니다
- 오류는 없습니다. 아직 호출을 하지 않았습니다

**4단계**

- `영상\` 폴더가 비어 있어 여기서 멈췄습니다

### 만들어진 파일

| 경로 | 크기 |
|---|---|
| `C:\골드TV\001-비우니-채워지더라\곡정보.json` | 1,935 바이트 |
| `C:\골드TV\001-비우니-채워지더라\업로드정보.json` | 1,563 바이트 |
| `C:\골드TV\001-비우니-채워지더라\썸네일.json` | 146 바이트 |
| `C:\골드TV\001-비우니-채워지더라\영상\` | 빈 폴더 |
| `youtube-goldtv\도구\api키.json` | 15 바이트 (커밋 안 됨) |

### 막힌 것

수노 API 호출을 아직 하지 않았으므로 **오류 메시지 없음**.
키가 채워진 뒤 다시 `/작업` 을 돌리면 그때 나오는 응답을 원문 그대로 여기에 남기겠습니다.

### 사용자가 해야 할 일

1. `youtube-goldtv\도구\api키.json` 을 열어 `"suno": ""` 의 따옴표 사이에
   수노 API 키를 넣기 — **키는 채팅창에 붙여넣지 마세요.** 파일에만 넣으시면 됩니다
2. 영상 클립을 `C:\골드TV\001-비우니-채워지더라\영상\` 에 넣기
   (`01.mp4`, `02.mp4`, `03.png` 처럼 이름순으로 배치됩니다)
3. 썸네일 배경 사진을 `C:\골드TV\001-비우니-채워지더라\배경.jpg` 로 넣기 (없으면 금빛 그러데이션으로 대체)
4. 유튜브 업로드까지 하려면 `client_secret.json` 을 `youtube-goldtv\도구\` 에 넣기
5. 제목을 1번 말고 다른 후보로 바꾸고 싶으면 알려주기

1번만 끝나면 3단계(노래 만들기)는 바로 돌릴 수 있습니다.

---

## (이전 기록 없음)

로컬에서 `/작업` 또는 `/보고` 를 한 번 실행하면 여기에 결과가 쌓입니다.
