# 지시사항

> 이 파일은 **클라우드 Claude(휴대폰·웹 대화)가 씁니다.**
> 파워셸이나 VS Code의 Claude에게 `/작업` 이라고 치면 이 파일대로 실행합니다.
>
> 끝난 항목은 `[x]` 로 바뀝니다.

**마지막 갱신**: 2026-09-16
**작성**: 클라우드 세션

---

## 지금 할 일

### 1단계 — 환경 준비

- [x] `python --version` 으로 파이썬이 깔려 있는지 확인. 없으면 사용자에게 알리고 여기서 멈춘다
- [x] `youtube-goldtv/도구/0-처음설치.bat` 실행 (pillow, 구글 라이브러리, ffmpeg 설치)
      — 꾸러미가 전부 이미 깔려 있어 bat 대신 개별 확인으로 갈음 (작업기록 참고)
- [x] `ffmpeg -version` 으로 설치 확인. 안 되면 명령 프롬프트를 새로 열어야 한다고 알린다
- [x] `youtube-goldtv/도구/api키.json` 이 있는지 확인.
      없으면 아래 내용으로 만들고, **키 값은 사용자가 직접 넣어야 한다**고 알린다
      ```json
      { "suno": "" }
      ```

### 2단계 — 첫 곡 작업 폴더 만들기

- [x] `C:\골드TV\001-비우니-채워지더라\영상\` 폴더를 만든다
- [x] `youtube-goldtv/02-가사/001-비우니-채워지더라.md` 의 **Song Title / Styles / Lyrics** 를 읽어서
      `C:\골드TV\001-비우니-채워지더라\곡정보.json` 을 만든다.
      형식은 `youtube-goldtv/도구/곡정보-예시.json` 과 똑같이 한다
- [x] `youtube-goldtv/03-업로드/001-비우니-채워지더라.md` 의 제목·설명·태그를 읽어서
      `업로드정보.json` 을 만든다. **공개범위는 반드시 `private`** 로 둔다
- [x] `썸네일.json` 을 만든다. 윗줄 `비우니`, 아랫줄 `채워지더라`, 아래작은글 `반야심경`

### 3단계 — 노래 만들기

- [ ] api키.json 에 키가 들어 있으면 `youtube-goldtv/도구/노래만들기.py` 를 실행한다
      ```
      python youtube-goldtv\도구\노래만들기.py "C:\골드TV\001-비우니-채워지더라"
      ```
- [x] **오류가 나면 화면에 나온 응답을 통째로** `작업기록.md` 에 남긴다.
      수노 API는 제공처마다 주소와 응답 형식이 달라서, 그 내용을 봐야 고칠 수 있다
      — 403 / 400 / 429 세 건 모두 원문 그대로 남겼습니다 (2026-09-16 16:21 기록).
      앞의 두 건은 고쳤고, 남은 것은 **크레딧 충전**뿐입니다
- [ ] 후보가 여러 개 받아졌으면 사용자에게 들어보고 하나를 `노래.mp3` 로 정하라고 알린다

### 4단계 — 여기서 멈춘다

- [ ] 영상 클립은 사용자가 직접 만들어 `영상\` 폴더에 넣어야 한다.
      **비어 있으면 영상 만들기를 실행하지 말고**, 클립을 넣어달라고 알린 뒤 멈춘다

---

## 다음에 할 일 (아직 시작하지 말 것)

- 영상 클립이 준비되면 `2-영상만들기` 실행
- 배경 사진이 준비되면 `3-썸네일만들기` 실행
- 구글 OAuth 파일이 준비되면 `4-업로드` 실행 (**반드시 비공개로**)

---

## 지켜야 할 것

- **API 키 값은 절대 출력하거나 기록하지 않는다.** 파일이 있는지 없는지만 확인한다
- mp3, mp4, 이미지는 커밋하지 않는다
- 업로드는 항상 `private` 로 먼저 한다. 공개 전환은 사용자가 직접 한다
- 막히면 넘겨짚지 말고 `작업기록.md` 에 오류 원문을 남기고 멈춘다
